{
  "name": "clientsidemiddleware",
  "version": "1.0.0",
  "description": "middleware mechanism",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rimraf dev && webpack --env.NODE_ENV=dev",
    "build": "rimraf dist &&  webpack --env.NODE_ENV=pro",
    "start": "webpack-dev-server --hot --inline"
  },
  "keywords": [
    "middleware"
  ],
  "author": "albert",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2",
    "webpack": "^3.8.1",
    "webpack-dev-server": "^2.9.4"
  }
}
